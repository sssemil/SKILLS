interface:
  display_name: "Brutal Worker"
  short_description: "One-ticket context-bounded implementation"
  default_prompt: "Use $brutal-worker to complete one exact BRUTAL.md task; in managed mode validate the manifest and execute only the supplied phase."
