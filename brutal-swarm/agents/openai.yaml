interface:
  display_name: "Brutal Swarm"
  short_description: "Small-context stacked-PR workers"
  default_prompt: "Use $brutal-swarm to drain a task graph through isolated workers with small phase context files."
