interface:
  display_name: "Brutal Swarm"
  short_description: "Context-bounded stacked-PR worker graph"
  default_prompt: "Use $brutal-swarm to drain a coordinated task graph through isolated workers with bounded phase manifests and materially clean review gates."
