interface:
  display_name: "Brutal Infinite Fix Loop"
  short_description: "Fix PRs until materially clean"
  default_prompt: "Use $brutal-inf-fix-loop to review and fix without a pass limit until a fresh pass has no CRITICAL or MAJOR findings."
